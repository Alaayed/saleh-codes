
The big comer himself.

Distributed mechanisms are more difficult than local mechanisms

Special hardware instruction that switches from user mode to [[OS Mode]] called: trap, sys call, and supervisor call.